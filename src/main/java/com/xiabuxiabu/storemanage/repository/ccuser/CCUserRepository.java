/*
package com.xiabuxiabu.storemanage.repository.ccuser;

import com.xiabuxiabu.storemanage.entity.ccuser.CCUser;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.data.jpa.repository.Query;
import org.springframework.data.repository.query.Param;

public interface CCUserRepository extends JpaRepository<CCUser,Integer> {
    @Query("from CCUser where username=:userName")
    CCUser findByUsername(@Param("userName") String userName);
}
*/
